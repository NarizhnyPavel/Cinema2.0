package Kinopoisk.client.calendar;

import Kinopoisk.client.connection.ConnectionManager;

import javax.swing.*;
import java.awt.*;

public class CalendarPanel extends JPanel
{
    /*public CalendarPanel ()
    {
        setLayout ( new BorderLayout () );
        final JButton button = new JButton ( "ВХОД" );
        button.addActionListener ( l -> {
            final String seanceId = ConnectionManager.getInstance ().getAuthService ().login ( "1", "1" );
            ConnectionManager.getInstance ().setSeanceId ( seanceId );
        } );
        add ( button );
    }*/
}
